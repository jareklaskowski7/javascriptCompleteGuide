const testResults = [1, 5.3, 1.5, 10.99, -5, 10];
// concat() adds elements to the end of the array
const storedResults = testResults.concat([3.99, 2]);

testResults.push(5.91);

console.log(testResults, storedResults);
